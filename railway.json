{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "cd backend && source /app/.venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}